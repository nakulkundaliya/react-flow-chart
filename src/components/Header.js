import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <div>
        <button>Save</button>
      </div>
    );
  }
}
export default Header;
